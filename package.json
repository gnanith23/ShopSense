{
  "name": "shopsense",
  "version": "1.0.0",
  "private": true,
  "description": "ShopSense Fullstack E-Commerce Application",
  "scripts": {
    "dev": "npx concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:all": "npx concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\" \"python -m uvicorn main:app --host 127.0.0.1 --port 8000 --app-dir realtime_service\"",
    "dev:backend": "npm start --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "npm start --prefix backend",
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "realtime": "python -m uvicorn main:app --host 127.0.0.1 --port 8000 --app-dir realtime_service"

  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@google/genai": "^2.15.0"
  }
}
