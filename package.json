{
  "name": "shopsense",
  "version": "1.0.0",
  "private": true,
  "description": "ShopSense Fullstack E-Commerce Application",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:all": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"python -m uvicorn main:app --host 127.0.0.1 --port 8000 --app-dir realtime_service\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "npm run dev --prefix backend",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "realtime": "python -m uvicorn main:app --host 127.0.0.1 --port 8000 --app-dir realtime_service"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@google/genai": "^2.15.0"
  }
}
